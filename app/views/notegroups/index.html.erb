<h1>Listing notegroups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notegroups.each do |notegroup| %>
  <tr>
    <td><%= link_to 'Show', notegroup %></td>
    <td><%= link_to 'Edit', edit_notegroup_path(notegroup) %></td>
    <td><%= link_to 'Destroy', notegroup, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Notegroup', new_notegroup_path %>
